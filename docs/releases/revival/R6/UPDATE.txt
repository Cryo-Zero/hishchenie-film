HISHCHENIE — REVIVAL R6 / PROFILE SAFETY + REPORT

Changes:
- Adds a familiar `?` control to the audience profile with a translucent browser-bound identity explanation.
- Keeps FAQ QUERY_08 title `На сайте есть безопасность?`; expands only its explanation.
- Adds REPORT // bug channel in the footer on main and reviews pages.
- Bug diagnostics contain only build/page/language/viewport/screen/user-agent/connectivity data; no UUID, alias or review text.
- Replaces arbitrary alias mixing with a curated bank of 98 meaningful RU/EN nickname bases; every newly generated nickname is `base + 3-digit number`.
- Legacy alias codes remain renderable so existing reviews keep their old names; `Тот, Кто Ждёт` and `Тот, Кто Помнит` are not generated by the new bank.
- Adds database-enforced unique visible alias identity; client transparently rerolls on a collision.
- Review deletion semantics are unchanged: replies and likes remain ON DELETE CASCADE.
- No approved desktop scene/grid behavior is removed or redesigned.

Backend:
- Supabase project `xltwwvutqkpmtmlavngi` was verified `ACTIVE_HEALTHY`.
- Migration `20260908155140_revival_r6_unique_aliases` is applied and verified.
- `profiles_unique_alias_identity_idx` exists as a verified UNIQUE index on `public.profiles`.
- Canonical duplicate groups after migration = 0; user-data table counts were unchanged during migration.
- Backend blocker is cleared.

Release state:
- R6 frontend remains in `revival-r6-profile-safety-report` / PR #17.
- Frontend is still not merged or published.
- Final PR/frontend verification remains before release.
